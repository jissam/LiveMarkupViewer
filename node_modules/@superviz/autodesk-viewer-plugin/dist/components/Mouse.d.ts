import { Slot } from '../types';
export declare class Mouse {
    constructor(viewer: any, userId: any);
    private clock;
    private destPos;
    private curScreenPos;
    slot: Slot;
    private userId;
    private renderFrame;
    private viewer;
    private mousesWrapper;
    load(name: string, slot: Slot): Promise<THREE.Object3D>;
    update: (destPos: THREE.Vector3, slot: Slot) => void;
    destroy(): void;
    private render;
    private renderDivWrapper;
}
