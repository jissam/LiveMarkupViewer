import { Slot } from '../types';
export declare class AvatarName {
    constructor(viewer: any, userId: any);
    private clock;
    private destPos;
    private curScreenPos;
    slot: Slot;
    private readonly userId;
    private renderFrame;
    private viewer;
    private avatarModel;
    load(avatarModel: THREE.Mesh, name: any, slot: Slot): void;
    private render;
    destroy(): void;
}
