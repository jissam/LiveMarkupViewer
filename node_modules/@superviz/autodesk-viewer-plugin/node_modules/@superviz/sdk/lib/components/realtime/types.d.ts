export declare enum RealtimeComponentState {
    STARTED = "STARTED",
    STOPPED = "STOPPED"
}
export declare enum RealtimeChannelState {
    DISCONNECTED = "DISCONNECTED",
    CONNECTED = "CONNECTED",
    CONNECTING = "CONNECTING"
}
export declare enum RealtimeComponentEvent {
    REALTIME_STATE_CHANGED = "realtime-component.state-changed"
}
export declare enum RealtimeChannelEvent {
    REALTIME_CHANNEL_STATE_CHANGED = "realtime-channel.state-changed"
}
export type RealtimeData = {
    name: string;
    payload: any;
};
export type RealtimeMessage = {
    name: string;
    connectionId: string;
    participantId: string | null;
    data: unknown;
    timestamp: number;
};
