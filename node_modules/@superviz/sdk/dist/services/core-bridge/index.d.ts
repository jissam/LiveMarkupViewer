import { Participant } from '../../common/types/participant.types';
declare class CoreBridge {
    private logger;
    constructor();
    updateLocalParticipant(data: Participant): void;
    updateParticipantsList(data: Record<Participant['id'], Participant>): void;
}
export declare const coreBridge: CoreBridge;
export {};
