import * as Socket from '@superviz/socket-client';
export declare class RealtimePresence {
    private room;
    private logger;
    constructor(room: Socket.Room);
    update<T = any>(data: T): void;
    subscribe<T = unknown>(event: Socket.PresenceEventsArg, callback: Socket.PresenceCallback<T>): void;
    unsubscribe(event: Socket.PresenceEventsArg): void;
    getAll(): Promise<Socket.PresenceEvent[]>;
}
